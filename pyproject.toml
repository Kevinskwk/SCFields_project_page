[build-system]
requires = ["setuptools>=64", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "scfields"
version = "0.1.0"
description = "Semantic-Contact Fields code release"
readme = "README.md"
requires-python = ">=3.8"
license = {text = "MIT"}
authors = [{name = "Kevin Yuchen Ma"}]

[tool.setuptools]
packages = []

[tool.ruff]
line-length = 120
target-version = "py38"
